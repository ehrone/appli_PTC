#:kivy 1.0

ScreenManager:

    acceuil:

        id:acceuil
        
        Label:
            font_size: 70
            text: " Welcome ! "

        Button:
            text: "Connexion"
            on_press :
                print("appuis connexion")

        Button:
            text: "Retour"
            on_press :
                print("appuis retour")
                app.change_window(nom_fenetre="acceuil)

    Log:
        id:log

        Label : 
            text : " Connexion "

        TextInput:
            text : " User id "

        TextInput:
            text : " User pwd "

        Button:
            text: "Connection"
            on_press :
                print("appuis retour")
                app.change_window(nom_fenetre="logged")

        Button:
            text: "Retour"
            on_press :
                print("appuis retour")
                app.change_window(nom_fenetre="acceuil")

    Logged:
        id:logged
        Button:
            text: "Retour log"
            on_press :
                print("appuis Retour")
                app.change_window(nom_fenetre="log")